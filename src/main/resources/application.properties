# General Application Properties
spring.application.name=employeetaskmanagement
server.port=8080

# Spring Security Properties
spring.security.user.name=admin
spring.security.user.password=adminpassword

# MongoDB Configuration
spring.data.mongodb.uri=mongodb://localhost:27017/employeetaskdb

# Hibernate/JPA Configuration (if needed)
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# Logging Configuration
logging.level.org.springframework=INFO
logging.level.com.example.employeetaskmanagement=DEBUG

# Email Configuration (if you are sending emails)
# spring.mail.host=smtp.example.com
# spring.mail.port=587
# spring.mail.username=email@example.com
# spring.mail.password=password
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true
